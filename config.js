/* Public config only. No 口令, no GitHub token.
   After you create the Worker, set WORKER_URL to the https://…workers.dev URL.
   After you create the 私有仓, set PRIVATE_REPO to "owner/repo" for the fallback Issue link. */
window.LEARN_ENGLISH = {
  WORKER_URL: "",
  PRIVATE_REPO: "ledahwong-lab/learn-english-private",
};
